import { ACTION_ADDTODO, ACTION_DELETETODO, ACTION_TOGGLEPROPERTY } from "../actions/todoActions";

const initialState = {
  todoData: []
}


const rootReducer = (state = initialState, { type, payload }) => {
  switch (type) { }
};